'use client';

/**
 * DEPRECATED MODULE: CLIENTS terminal has been decommissioned.
 * All client discovery now occurs contextually through KPI and Spend terminals.
 */
export default function DecommissionedClientsPage() {
  return null;
}
